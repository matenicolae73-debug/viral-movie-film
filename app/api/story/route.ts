import {NextResponse} from "next";
export async function POST(req:Request){const b=await req.json().catch(()=>({}));const idea=String(b?.idea||"").trim();const genre=String(b?.genre||"Cinematic");const minutes=Number(b?.minutes||1);if(!idea)return NextResponse.json({error:"Idea is required."},{status:400});return NextResponse.json({ok:true,demo:true,title:"Untitled ViralMovie",logline:`A ${genre.toLowerCase()} story built from: ${idea}`,sceneCount:Math.max(12,Math.round(minutes*12)),note:"Story planning is ready. Connect an LLM provider for fully generated scripts."})}