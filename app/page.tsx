 "use client";
import {useState} from "react";
const durations=[1,3,5,10,30,60];
export default function Home(){const[idea,setIdea]=useState("");const[genre,setGenre]=useState("Cinematic");const[minutes,setMinutes]=useState(1);const[status,setStatus]=useState("Ready to create your movie.");const[story,setStory]=useState<any>(null);
async function generateStory(){if(!idea.trim()){setStatus("Write your movie idea first.");return}setStatus("Planning your movie...");try{const r=await fetch("/api/story",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({idea,genre,minutes})});const d=await r.json();if(!r.ok)throw new Error(d.error);setStory(d);setStatus(`Movie plan ready: about ${d.sceneCount} scenes for ${minutes} minute(s).`)}catch(e:any){setStatus(e.message||"Something went wrong.")}}
return <main className="wrap"><nav className="nav"><div className="logo">🎬 ViralMovie AI</div><div className="pill">English • AI Movie Studio</div></nav><section className="hero"><h1>Turn one idea into a movie.</h1><p>Create a cinematic project from a simple idea. Choose the length, plan the story, split it into scenes, generate AI video and build the final movie.</p></section><section className="grid"><div className="card"><h2>Create Movie</h2><label className="label">Movie idea</label><textarea className="textarea" value={idea} onChange={e=>setIdea(e.target.value)} placeholder="Example: A young astronaut lands on Mars and discovers an abandoned city..." /><label className="label">Genre</label><select className="select" value={genre} onChange={e=>setGenre(e.target.value)}>{["Cinematic","Action","Drama","Sci-Fi","Horror","Comedy","Fantasy","Thriller"].map(x=><option key={x}>{x}</option>)}</select><label className="label">Movie duration</label><div className="durations">{durations.map(x=><button key={x} className={`duration ${minutes===x?"active":""}`} onClick={()=>setMinutes(x)}>{x} min</button>)}</div><button className="primary" onClick={generateStory}>Generate Movie Plan</button><div className="status">{status}</div>{story&&<div className="card" style={{marginTop:16,padding:16}}><b>{story.title}</b><p>{story.logline}</p><div className="mini">{story.sceneCount} planned scenes • approximately {minutes*60} seconds</div></div>}</div><aside className="card"><h2>Movie Pipeline</h2><div className="steps">{[["1. IDEA","Your concept"],["2. AI STORY","Plot, scenes and continuity"],["3. CHARACTERS","Consistent characters"],["4. STORYBOARD","Automatic scene breakdown"],["5. AI VIDEO","5-second scene generation"],["6. AUDIO","Dialogue, narration, SFX and music"],["7. MOVIE","Scenes assembled into your selected duration"],["8. EXPORT","Ready for social or download"]].map(([a,b])=><div className="step" key={a}><b>{a}</b><div className="mini">{b}</div></div>)}</div></aside></section><footer className="footer">ViralMovie AI V2 • Standalone project • FAL/Vidu integration uses the private FAL_KEY server-side.</footer></main>}