"use client";
import "./theme.css";
import { useEffect, useState } from "react";
type Movie={slug:string;title:string;description:string;videoUrl:string;publishedAt:string;aiGenerated?:boolean};
export default function Movies(){const [movies,setMovies]=useState<Movie[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{fetch('/api/movies',{cache:'no-store'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Movies unavailable');setMovies(d.movies||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="public-page"><header className="public-nav"><a href="/" className="brand-link">🎬 <b>ViralMovie</b> <span>STREAM</span></a><nav><a href="/">Studio</a><a className="active" href="/movies">Watch</a><a href="/credits">Credits</a></nav></header><section className="public-hero"><div className="hero-kicker">VIRALMOVIE STREAM</div><h1>Discover AI movies.</h1><p>Original stories, cinematic worlds and AI-generated films — curated by ViralMovie.</p><a className="hero-cta" href="/">✦ Create a movie</a></section><section className="movie-catalog"><div className="section-head"><h2>Featured Films</h2><span>{movies.length} available</span></div>{loading?<div className="info-box">Loading the cinema...</div>:error?<div className="error-box">{error}<br/><small>Connect Upstash Redis to enable the public catalog.</small></div>:movies.length===0?<div className="empty-catalog"><div className="empty-icon">▶</div><h3>Your cinema is ready for its first premiere.</h3><p>The owner can publish finished films from the Studio. New releases will appear here.</p><a className="watch" href="/">Create the first film</a></div>:<div className="catalog-grid">{movies.map(m=><article className="catalog-card" key={m.slug}><div className="poster-wrap"><video muted playsInline preload="metadata" src={m.videoUrl}/><div className="poster-play">▶</div><span className="ai-badge">AI FILM</span></div><div className="catalog-body"><h3>{m.title}</h3><p>{m.description}</p><a href={`/movie/${m.slug}`} className="watch">▶ Watch now</a></div></article>)}</div>}</section><footer>ViralMovie Stream • Original AI cinema • <a href="/terms">Terms</a> · <a href="/privacy">Privacy</a> · <a href="/acceptable-use">Acceptable Use</a></footer></main>}
