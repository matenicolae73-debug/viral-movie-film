"use client";
import { useEffect, useState } from "react";
type Movie={slug:string;title:string;description:string;videoUrl:string;publishedAt:string;aiGenerated?:boolean};
export default function Movies(){const [movies,setMovies]=useState<Movie[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{fetch('/api/movies',{cache:'no-store'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Movies unavailable');setMovies(d.movies||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="public-page"><header className="public-nav"><a href="/" className="brand-link">🎬 ViralMovie <span>AI</span></a><nav><a href="/">Studio</a><a href="/movies">Movies</a><a href="/credits">Credits</a></nav></header><section className="public-hero"><div className="hero-kicker">VIRALMOVIE ORIGINALS</div><h1>AI Movies, made for the world.</h1><p>Watch original AI-generated films curated and published by ViralMovie AI. Every published movie is reviewed before it appears here.</p><a className="hero-cta" href="/">Create your own video</a></section><section className="movie-catalog"><div className="section-head"><h2>🎞️ Movies</h2><span>{movies.length} published</span></div>{loading?<div className="info-box">Loading movies...</div>:error?<div className="error-box">{error}<br/><small>Connect Upstash Redis to enable the public catalog.</small></div>:movies.length===0?<div className="empty-catalog"><h3>The first ViralMovie is coming soon.</h3><p>The owner can publish finished movies from the Studio.</p></div>:<div className="catalog-grid">{movies.map(m=><article className="catalog-card" key={m.slug}><video muted playsInline preload="metadata" src={m.videoUrl}/><div className="catalog-body"><span className="ai-badge">AI GENERATED</span><h3>{m.title}</h3><p>{m.description}</p><a href={`/movie/${m.slug}`} className="watch">▶ Watch Movie</a></div></article>)}</div>}</section><footer>ViralMovie AI • Movies are curated by the owner • <a href="/terms">Terms</a> · <a href="/privacy">Privacy</a> · <a href="/acceptable-use">Acceptable Use</a></footer></main>}
